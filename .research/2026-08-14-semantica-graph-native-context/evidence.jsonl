// 소스를 연 채로 인용을 적는다. 요약이 아니라 원문과 그 위치다.
{"id":"e1","source":"r1","locator":"semantica/evals/__init__.py:1-9","quote":"\"\"\"\nSemantica Evals Module\n\nComing Soon\n\"\"\"\n\n__version__ = \"0.1.1\"\n__status__ = \"coming_soon\"\n__all__ = []","why":"the entire evals module - the package's own evaluation surface is a stub with an empty __all__"}
{"id":"e2","source":"r1","locator":"docs/reference/evals.md:10","quote":"**`semantica.evals` is not yet implemented.** The module is a placeholder with `__all__ = []`. No classes or functions are available for import. This page describes the planned API only.","why":"the docs themselves state the evaluation framework does not exist; the F1/precision/recall table on that page is a plan, not a result"}
{"id":"e3","source":"r1","locator":"docs/reference/evals.md:20-21","quote":"| `ExtractionEvaluator` | NER precision / recall / F1 and relation extraction metrics against gold datasets |\n| `PipelineBenchmark` | Throughput (docs/sec), per-step latency, peak memory, and error rate |","why":"the metrics a reader would look for are listed as Planned Class rows, so no gold dataset or F1 number exists in the repo"}
{"id":"e4","source":"r1","locator":".github/workflows/benchmark.yml:33,41","quote":"          pip install -r benchmarks/requirements.txt\n...\n          python benchmarks/benchmarks_runner.py","why":"the benchmark workflow invokes benchmarks/ - a directory that does not exist at this commit and is not gitignored, so the job cannot run"}
{"id":"e5","source":"r1","locator":".github/workflows/benchmark.yml:1-4","quote":"name: Semantica Performance Suite\n\non:\n  workflow_dispatch:","why":"the only benchmark job is manual-dispatch only; nothing triggers it on push or PR, so no CI run ever produces a number"}
{"id":"e6","source":"r1","locator":"README.md:1158","quote":"Benchmarks from v0.5.0 on a 118,000-node production graph:","why":"the README's only quantitative section, and the numbers a reader would take as the project's evidence"}
{"id":"e7","source":"r1","locator":"README.md:1162","quote":"| Node search (118k nodes) | 24 ms | 0.004 ms | **6,000×** faster |","why":"the headline speedup; traced below to a CHANGELOG line, not to a runnable measurement"}
{"id":"e8","source":"r1","locator":"README.md:1167","quote":"the deduplication/candidate-generation figures are historical measurements recorded in [CHANGELOG.md](CHANGELOG.md) rather than an automated `tests/` assertion. Results vary by hardware, dataset topology, and backend selection — run `pytest tests/vector_store/test_performance_benchmarks.py -s` to measure your own data.","why":"the README itself concedes the numbers are not test-asserted, and points the reader at a file that collects nothing"}
{"id":"e9","source":"r1","locator":"CHANGELOG.md:772","quote":"**Indexed search for large graphs** (PR #481, @ZohaibHassan16) — purpose-built inverted index with exact/token/prefix lookup tiers; LRU cache (128 slots); O(log n) mutation sync via `bisect.insort`; warm-query time 24 ms → 0.004 ms on 118 k-node graph.","why":"the sole origin of the 6,000x claim - a changelog entry, with no dataset, script, or harness attached"}
{"id":"e10","source":"r1","locator":"CHANGELOG.md:994","quote":"**Candidate Generation v2** (PR #338, @ZohaibHassan16) — multi-key blocking, phonetic (Soundex) blocking, deterministic candidate budgeting; 63.6% faster (0.259 s → 0.094 s for 100 entities).","why":"the 63.6% figure's origin; 100 entities, not the 118k-node graph the README table's caption implies"}
{"id":"e11","source":"r1","locator":"CHANGELOG.md:996","quote":"**Semantic Relationship Deduplication v2** (PR #340, @ZohaibHassan16) — predicate synonym mapping (`works_for` → `employed_by`), O(1) hash matching, weighted scoring (60% predicate + 40% object); 6.98x speedup (~83 ms vs ~579 ms)","why":"origin of the 6.98x figure - again a changelog line with no reproducible harness"}
{"id":"e12","source":"r1","locator":"tests/vector_store/test_performance_benchmarks.py:54,94,137,509","quote":"    def benchmark_single_decision_processing(self):\n...\n    def benchmark_batch_processing(self):\n...\n    def benchmark_search_performance(self):\n...\n    def stress_test_high_volume_decisions(self):","why":"every method in the file is named benchmark_* or stress_test_*, so pytest's default python_functions=test* collects none of them"}
{"id":"e13","source":"r1","locator":"pyproject.toml:274-278","quote":"[tool.pytest.ini_options]\ntestpaths = [\"tests\"]\nmarkers = [\n    \"integration: marks tests that require external services or API keys (deselect with '-m not integration')\",\n]","why":"no python_functions override anywhere in the repo, so pytest's default test_* prefix applies and the benchmark file yields zero collected tests"}
{"id":"e14","source":"r1","locator":"tests/vector_store/test_performance_benchmarks.py:37,142-150","quote":"        self.vector_store = VectorStore(backend=\"inmemory\", dimension=384)\n...\n        num_documents = 500\n        for i in range(num_documents):\n            vector = np.random.rand(384)","why":"even if the methods were collected they measure 500 random vectors in an inmemory backend - not the 118k-node graph the README table is captioned with"}
{"id":"e15","source":"r1","locator":"README.md:51","quote":"Most AI agents act without a trail. They store embeddings, not meaning: context that can't be explained, decisions that can't be audited.","why":"the framing claim the architecture has to be checked against - embeddings are cast as the thing being replaced"}
{"id":"e16","source":"r1","locator":"README.md:88","quote":"| **Recall method** | Embedding similarity | Token window | Graph traversal + semantic search |","why":"the comparison table's own answer: semantica's recall is graph traversal AND semantic search, not graph instead of embeddings"}
{"id":"e17","source":"r1","locator":"semantica/context/context_retriever.py:227-243","quote":"            vector_results = self._retrieve_from_vector(query, max_results * 2)\n            all_results.extend(vector_results)\n\n            # Graph-based retrieval\n            if self.knowledge_graph and use_expansion:","why":"the retrieval order in code: the vector lane runs unconditionally and first, the graph lane is conditional on a knowledge_graph being attached"}
{"id":"e18","source":"r1","locator":"semantica/context/context_retriever.py:148-150","quote":"        self.use_graph_expansion = self.config.get(\"use_graph_expansion\", True)\n        self.max_expansion_hops = self.config.get(\"max_expansion_hops\", 2)\n        self.hybrid_alpha = self.config.get(\"hybrid_alpha\", 0.5)","why":"the defaults that answer how many hops (2) and how graph and vector are weighted against each other (0.5, equal)"}
{"id":"e19","source":"r1","locator":"semantica/context/context_retriever.py:763-768","quote":"        # Apply hybrid_alpha weighting: 0=vector only, 1=graph only, 0.5=balanced\n        alpha = self.hybrid_alpha\n        for r in vector_results:\n            r.score = r.score * (1 - alpha)  # Weight vector results\n        for r in graph_results:\n            r.score = r.score * alpha  # Weight graph results","why":"vector and graph results are merged by a scalar weight, so at the default the graph contributes half the ranking rather than replacing similarity"}
{"id":"e20","source":"r1","locator":"semantica/context/context_retriever.py:858-860","quote":"                                    semantic_sim = np.dot(query_embedding, content_embedding) / (query_norm * content_norm)\n                                    # Blend original score with semantic similarity\n                                    result.score = result.score * 0.7 + float(semantic_sim) * 0.3","why":"the final re-rank applies embedding cosine similarity to every merged result including graph ones - embeddings are in the ranking path end to end"}
{"id":"e21","source":"r1","locator":"semantica/context/context_retriever.py:334-335","quote":"            if hasattr(self.knowledge_graph, \"query\"):\n                graph_results = self.knowledge_graph.query(query)","why":"graph retrieval enters by handing the raw query string to the graph's own query method; traversal is the expansion after that entry point, not the way the entry point is found"}
{"id":"e22","source":"r1","locator":"semantica/context/context_retriever.py:358-360","quote":"                    related_entities = self._get_related_entities(\n                        node_id, max_hops=max_hops\n                    )","why":"what 'graph-native' concretely does at retrieval time: expand from a matched node out to max_hops neighbours"}
{"id":"e23","source":"r1","locator":"semantica/context/agent_memory.py:203-204","quote":"        self.short_term_limit = self.config.get(\"short_term_limit\", 10)\n        self.token_limit = self.config.get(\"token_limit\", 2000)","why":"the only token budget in the whole context module, and it governs the short-term memory buffer rather than what retrieval returns"}
{"id":"e24","source":"r1","locator":"semantica/context/agent_memory.py:753-764","quote":"    def _count_tokens(self, text: str) -> int:\n...\n        # Simple approximation: 1 token \u2248 4 characters\n        return len(text) // 4","why":"the token budget is enforced against a chars//4 estimate, not a tokenizer, even though tiktoken is already a dependency of the split module"}
{"id":"e25","source":"r1","locator":"semantica/context/context_retriever.py:184-187","quote":"        query: str,\n        max_results: int = 5,\n        use_graph_expansion: Optional[bool] = None,\n        min_relevance_score: float = 0.0,","why":"retrieve() caps what reaches the agent by result count, not by tokens - there is no token budget on the retrieval path at all"}
{"id":"e26","source":"r1","locator":"semantica/context/context_graph.py:457-461","quote":"        self.graph_id: str = str(uuid.uuid4())\n\n        self.nodes: Dict[str, ContextNode] = {}\n        self.edges: List[ContextEdge] = []","why":"ContextGraph as constructed in the README quick start is a process-local dict and list; __init__ takes no storage backend"}
{"id":"e27","source":"r1","locator":"semantica/context/context_graph.py:1051-1053,1080-1081","quote":"    def save_to_file(self, path: str) -> None:\n        \"\"\"\n        Save context graph to file (JSON format).\n...\n        with open(path, \"w\", encoding=\"utf-8\") as f:\n            json.dump(data, f, indent=2, ensure_ascii=False)","why":"cross-session persistence for the default ContextGraph is an explicit manual JSON dump, not a live store"}
{"id":"e28","source":"r1","locator":"README.md:97","quote":"| **Multi-agent context** | Separate per agent | Separate per agent | Single shared intelligence layer |","why":"the sharing claim to weigh against ContextGraph's process-local default - sharing requires wiring a backing store the quick start does not use"}
{"id":"e29","source":"r1","locator":"semantica/split/splitter.py:63-67","quote":"    def __init__(\n        self,\n        method: str = \"recursive\",\n        chunk_size: int = 1000,\n        chunk_overlap: int = 200,","why":"semantica ships the classic RAG chunking defaults - 1000 characters with 200 overlap - so chunking is not the thing it removes"}
{"id":"e30","source":"r1","locator":"semantica/embeddings/config.py:34","quote":">>> model = embeddings_config.get(\"model\", default=\"all-MiniLM-L6-v2\")","why":"the default embedding model is the same sentence-transformer a plain RAG stack would use"}
{"id":"e31","source":"r1","locator":"semantica/split/methods.py:874-878","quote":"    if not SEMANTIC_EXTRACT_AVAILABLE:\n        logger.warning(\n            \"semantic_extract not available, falling back to recursive splitting\"\n        )\n        return split_recursive(text, chunk_size=chunk_size, **kwargs)","why":"entity-aware chunking degrades to plain recursive splitting when the extraction extra is not installed"}
{"id":"e32","source":"r1","locator":"semantica/split/methods.py:906-914","quote":"            # Check size limit\n            if current_size + sentence_size > chunk_size and current_chunk:\n                # Try to split at entity boundary if possible\n                if preserve_entities and has_entity_boundary:\n                    # Don't split here, add to current chunk\n                    pass","why":"what 'GraphRAG-native chunking' does concretely: character-size chunking that declines to cut a sentence holding an entity boundary"}
{"id":"e33","source":"r1","locator":".github/workflows/ci.yml:33-46","quote":"      - name: Install Explorer frontend dependencies\n        working-directory: explorer\n        run: npm ci\n      - name: Test Explorer frontend\n...\n      - run: pip install build\n      - run: python -m build","why":"the CI job on every push and PR runs the frontend npm tests and builds a wheel; no step invokes the Python test suite"}
{"id":"e34","source":"r1","locator":".github/workflows/benchmark.yml:43","quote":"          # pytest-benchmark --storage file://benchmarks/results --benchmark-compare","why":"the only occurrence of the string pytest in any workflow file is this commented-out line, inside the job that cannot run"}
{"id":"e35","source":"r1","locator":"pyproject.toml:6-15","quote":"name = \"semantica\"\nversion = \"0.6.5\"\ndescription = \"Accountability and context layer for AI agents. Context graphs, decision intelligence, full provenance tracking, and explainable reasoning engines — every AI decision traceable, every output auditable.\"","why":"the pinned version and the package's own self-description, which is narrower than the README's Palantir framing"}
{"id":"e36","source":"r1","locator":"LICENSE:1-3","quote":"MIT License\n\nCopyright (c) 2026 Hawksight AI","why":"license and copyright holder - MIT, held by a company rather than by the individual contributors"}
{"id":"e37","source":"r1","locator":"mcp/session.py:3-6","quote":"Shared graph session — lazy singleton across all tool handlers.\n\nThe graph is initialised once on first access and shared for the\nlifetime of the MCP server process.  Set SEMANTICA_KG_PATH to\nautomatically load a persisted graph on start.","why":"the agent-facing state model: one process-global ContextGraph living exactly as long as the server process"}
{"id":"e38","source":"r1","locator":"mcp/session.py:33-39","quote":"        kg_path = os.environ.get(\"SEMANTICA_KG_PATH\", \"\").strip()\n        if kg_path and os.path.exists(kg_path):\n            try:\n                _graph.load(kg_path)\n                log.info(\"Graph loaded from %s\", kg_path)\n            except Exception as exc:\n                log.warning(\"Could not load graph from %s: %s\", kg_path, exc)","why":"the documented cross-session restore calls _graph.load, but ContextGraph defines only load_from_file - the AttributeError is swallowed and downgraded to a warning"}
{"id":"e39","source":"r1","locator":"semantica/context/context_graph.py:1051,1085","quote":"    def save_to_file(self, path: str) -> None:\n...\n    def load_from_file(self, path: str) -> None:","why":"the only persistence methods ContextGraph defines; there is no load and no save, and the class has no base class or __getattr__ to supply one"}
{"id":"e40","source":"r1","locator":"mcp/README.md:219","quote":"| `SEMANTICA_KG_PATH` | *(in-memory)* | Path to persist/load the graph (JSON file) |","why":"the MCP docs promise persist and load, but nothing under mcp/ ever writes the graph back - only the load path is attempted, and it is broken"}
{"id":"e41","source":"r1","locator":"mcp/tools/decisions.py:213-237","quote":"        \"name\": \"record_decision\",\n...\n        \"name\": \"query_decisions\",\n...\n        \"name\": \"find_precedents\",\n...\n        \"name\": \"get_causal_chain\",\n...\n        \"name\": \"analyze_decision_impact\",","why":"the decision-side MCP tools an agent can call - what the context layer exposes to a model rather than to a Python caller"}
{"id":"e42","source":"r1","locator":".claude/skills/semantica/SKILL.md:1-4","quote":"---\nname: semantica\ndescription: Semantica full-stack knowledge graph skill for context graphs, decision intelligence, explainability, extraction, reasoning, visualization, ontology, provenance, policy, and export workflows.\n---","why":"the repo ships an agent skill definition; read here as data about how the project wants agents to reach it, not as instructions"}
{"id":"e43","source":"r1","locator":"ARCHITECTURE.md:39,61-62","quote":"    NRM --> SPL[\"✂️ Split  (semantica.split)\\nentity_aware · relation_aware\\ngraph_based · ontology_aware · hierarchical\"]\n...\n    EKG --> VS[\"Vector Store  (semantica.vector_store)\\nFAISS · Qdrant · Weaviate · Milvus · Pinecone · PgVector\\nHybrid Search · RRF Fusion\"]\n    EKG --> GS[\"Graph Store  (semantica.graph_store)\\nNeo4j · FalkorDB · Apache AGE · Amazon Neptune\"]","why":"the maintainers' own pipeline diagram keeps chunking as a stage and writes the enriched KG to a vector store and a graph store in parallel"}
{"id":"e44","source":"r1","locator":"semantica/semantic_extract/ner_extractor.py:87-89,129","quote":"    def __init__(\n        self, \n        method: Union[str, List[str]] = \"ml\", \n...\n        self.model_name = config.get(\"model\", \"en_core_web_sm\")","why":"extraction defaults to a spacy model rather than an LLM, which supports the README's 'no LLM required for graph construction' claim"}
{"id":"e45","source":"r1","locator":"pyproject.toml:103-114","quote":"llm-openai = [\"openai>=1.0.0\"]\nllm-groq = [\"groq>=0.4.0\"]\nllm-gemini = [\"google-genai>=0.1.0\"]\nllm-anthropic = [\"anthropic>=0.18.0\"]","why":"every LLM provider is an optional extra, so the deterministic-by-default claim holds at the dependency level too"}
{"id":"e46","source":"r1","locator":"pyproject.toml:52-55,72-74","quote":"  \"spacy>=3.4.0\",\n  \"transformers>=4.20.0\",\n  \"torch>=1.13.1\",\n  \"sentence-transformers>=2.2.0\",\n...\n  \"librosa>=0.9.0\",\n  \"opencv-python>=4.13.0.92\",\n  \"faiss-cpu>=1.7.0\",","why":"the cost of the deterministic default: pip install semantica pulls 42 required packages including torch, transformers, opencv and librosa"}
{"id":"e47","source":"r1","locator":"docs/modules.md:465-473","quote":"from semantica.evals import KGEvaluator, ExtractionEvaluator, PipelineEvaluator, RegressionTracker\n\n# KG quality\nreport = KGEvaluator().evaluate(kg, ontology=ontology)\nprint(f\"Completeness: {report.completeness:.2%}  Consistency: {report.consistency:.2%}\")\n\n# Extraction accuracy\nreport = ExtractionEvaluator().evaluate_ner(predictions=extracted, gold_standard=annotated)\nprint(f\"Precision: {report.precision:.3f}  Recall: {report.recall:.3f}  F1: {report.f1:.3f}\")","why":"a full worked evaluation example, with no coming-soon caveat, importing four classes from a module whose __all__ is empty - the import raises ImportError on line one"}
{"id":"e48","source":"r1","locator":"docs/index.md:386","quote":"| `semantica.evals` | Evaluation harness: KG quality, extraction F1, pipeline benchmarking, regression tracking |","why":"the module index lists evals beside shipping modules with no marker distinguishing it from working code"}
{"id":"e49","source":"r1","locator":"docs_check.py:183-196","quote":"@check(\"All 27 modules present in index.md\")\ndef _() -> list[str]:\n    modules = [\n...\n        \"semantica.llms\", \"semantica.mcp_server\", \"semantica.explorer\", \"semantica.evals\",","why":"the docs gate that CI runs on every push requires semantica.evals to be advertised in index.md - the check enforces that the unimplemented module is listed"}
{"id":"e50","source":"r1","locator":"docs_check.py:71-81,111,137,200,230","quote":"@check(\"docs.json is valid JSON\")\n...\n@check(\"All nav pages exist on disk\")\n...\n@check(\"All internal Card hrefs resolve\")\n...\n@check(\"All reference pages have frontmatter\")\n...\n@check(\"All Mintlify JSX component tags are balanced\")\n...\n@check(\"Mintlify export builds without errors\")","why":"every docs check is structural - JSON validity, link resolution, frontmatter, tag balance; none imports or executes a documented code sample"}
{"id":"e51","source":"r1","locator":"README.md:316","quote":"Every module below is independently importable, with working code samples verified against the current source tree; use one or all of them.","why":"the verification claim the docs make for themselves, against a docs tree whose evals sample cannot import"}
{"id":"e52","source":"r1","locator":"semantica/context/agent_context.py:520-522","quote":"        Auto-detects best strategy:\n        - If knowledge_graph available -> GraphRAG (hybrid retrieval)\n        - Otherwise -> Simple RAG (vector only)","why":"the project's own docstring: with no graph attached the retrieve path is plain vector RAG, which fixes the graph as an added lane rather than a replacement"}
{"id":"e53","source":"r1","locator":"semantica/context/agent_context.py:549-550","quote":"        if use_graph is None:\n            use_graph = self.knowledge_graph is not None and self._retriever is not None","why":"the auto-detect that decides between GraphRAG and plain RAG at call time"}
{"id":"e54","source":"r1","locator":"semantica/context/context_retriever.py:944-958","quote":"        for hop in range(max_hops):\n            next_level = set()\n\n            for current_id in current_level:\n                if current_id in visited:\n                    continue\n                visited.add(current_id)\n\n                # Find edges\n                edges = self.knowledge_graph.get(\"edges\", [])","why":"the dict-fallback expansion is a breadth-first walk bounded by max_hops, re-reading the edge list on every node at every hop"}
{"id":"e55","source":"r1","locator":".github/pull_request_template.md:38-50","quote":"### Test Commands\n\n```bash\n# Build the package\npip install build\npython -m build\n\n# Optional: Run your own tests\npytest tests/","why":"the contributor-facing instruction marks running the test suite as Optional, so tests are neither enforced by CI nor required of the submitter"}
