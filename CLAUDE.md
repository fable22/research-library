@AGENTS.md

## Claude Code

The three skills above live in `.claude/skills/` and load through the Skill tool, so name
the task rather than the file: "start research on <url>" reaches `research-source`.
Other agents have to open `SKILL.md` directly.
