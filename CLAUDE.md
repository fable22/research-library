# CLAUDE.md

이 저장소에서 리서치 문서를 만들 때 따를 지침이다. 사람이 읽는 저장소 소개는 `README.md` 에 있고, 발행된 문서 목록은 `index.html` 이 담당한다. 이 파일은 작업 절차만 다룬다.

## 이 저장소가 하는 일

논문, 기술, 주제를 조사해서 **HTML 문서 한 파일**로 만들고 GitHub Pages 로 공개한다. 문서는 읽는 사람이 원문을 다시 찾아보지 않아도 판단할 수 있을 만큼 구체적이어야 하고, 동시에 어디까지가 원문이고 어디부터가 해석인지 구분되어야 한다.

## 새 문서 추가 절차

```bash
mkdir -p research/YYYY-MM-DD-slug
# index.html 과 meta.json 을 그 안에 작성
node scripts/check-doc.mjs research/YYYY-MM-DD-slug   # 규칙 위반 검사
node scripts/build-index.mjs                          # 목록 갱신
git add -A && git commit && git push
```

푸시 후 1~2분이면 https://fable22.github.io/research-library/ 에 반영된다.

디렉터리 이름은 `YYYY-MM-DD-slug`. 날짜가 앞에 있어야 파일 목록이 시간순이 되고 URL 만 봐도 언제 것인지 알 수 있다. slug 는 영문 소문자와 하이픈만 쓴다.

`meta.json` 필드는 `README.md` 의 표를 따른다. `title` 과 `date` 는 필수다.

## 같은 연구 흐름을 다룰 때

한 편으로 묶을지 나눌지는 **논문의 성격이 다른가**로 판단한다. 방법론 논문과 시스템 논문을 한 문서에 넣으면 둘 다 뭉개진다. 반대로 한 논문이 다른 논문의 한 절을 확장한 정도라면 굳이 나누지 않는다.

나눴다면 반드시 연결한다.

- `meta.json` 에 같은 `series` 값을 넣는다. 목록에서 묶여 보인다.
- 각 문서 표지에 **계보 바**(`.lineage`)를 넣어 앞뒤 문서로 이동할 수 있게 한다. 현재 문서는 `.self`, 나머지는 링크다.
- 앞 문서에서 "이건 확인되지 않았다"고 적은 항목을 뒤 문서가 채웠다면, **앞 문서로 돌아가 그 자리에 링크를 건다.** 앞 문서를 그대로 두면 읽는 사람이 낡은 결론을 그대로 가져간다.
- 문서 간 링크는 `../<slug>/#p<장번호>` 형식으로 특정 장을 가리킨다. 장 번호가 바뀌면 링크도 고친다.

## 조사 단계에서 지킬 것

**원문을 직접 읽는다.** 논문이면 arXiv HTML 전문을 받아서 본문과 부록과 표를 다 확인한다. 초록이나 남의 요약글만 보고 쓰지 않는다. 요약 도구가 돌려준 수치는 표에서 다시 확인한다.

**2차 출처의 수치는 원문과 대조한다.** 블로그나 커뮤니티 소개글의 숫자가 원문과 다른 경우가 흔하다. 다르면 문서 안에 어느 쪽이 맞는지 밝힌다.

**원문에 없는 계산을 했으면 표시한다.** 표 두 개를 겹쳐 새 비교를 만들었다면 캡션에 "논문에는 이 비교가 없다"고 적는다. 독자가 원문에서 찾다가 없어서 혼란스러워하는 일을 막는다.

**한계를 별도 항목으로 다룬다.** 결과만 옮기면 자료의 값이 떨어진다. 통제되지 않은 조건, 비용 미보고, 자체 벤치마크, 재현 가능 여부를 확인하고 근거와 함께 적는다. 반대 결과를 보고한 연구가 있으면 찾아서 같이 싣는다.

## 문서 작성 규칙

**외부 리소스를 불러오지 않는다.** CSS 와 JS 는 인라인, 이미지는 `data:` URI, 폰트는 시스템 폰트 스택만 쓴다. CDN 링크는 쓰지 않는다. 파일 하나만 있으면 오프라인에서 열려야 하고, 저장소를 옮겨도 깨지지 않아야 한다. 본문 안의 출처 링크(`<a href>`)는 클릭용이므로 예외다.

**완전한 HTML 문서로 만든다.** `<!doctype html>`, `<html lang="ko">`, `<meta charset="utf-8">`, viewport meta 를 포함한다. doctype 이 없으면 브라우저가 quirks mode 로 렌더링해서 레이아웃이 어긋난다.

**라이트와 다크 두 테마를 지원한다.** `@media (prefers-color-scheme: dark)` 로 OS 설정을 따른다. 색은 CSS 커스텀 프로퍼티로 정의하고 컴포넌트는 토큰만 참조한다.

**넓은 요소는 자체 스크롤을 준다.** 표, 코드 블록, 차트는 `overflow-x: auto` 컨테이너 안에 넣는다. 페이지 본문이 가로로 스크롤되면 안 된다.

**이미지에 alt 텍스트를 쓴다.** 논문 그림을 넣을 때는 그림이 무엇을 보여주는지 문장으로 서술한다. "그림 1" 같은 라벨만 넣지 않는다.

**이미지를 넣기 전에 압축한다.** `data:` URI 는 base64 라 원본보다 약 33% 커진다. 문서 하나가 1MB 를 넘기 시작하면 저장소가 금방 무거워진다. 표시 폭에 맞춰 리사이즈하고 PNG 는 팔레트를 줄인 뒤 넣는다. 직접 그릴 수 있는 도표는 이미지 대신 인라인 SVG 로 만든다.

## 문안 규칙

**기술 용어는 영어를 그대로 쓴다.** RAG, embedding, chunk, corpus, baseline, ablation, agent, tool call, F1, multi-hop 등은 번역하지 않는다. 억지로 옮긴 조어는 오히려 읽기 어렵다.

**비유를 만들지 않는다.** "지식베이스가 썩는다", "구조를 걸어다닌다", "어디서 점수를 벌었는지" 같은 표현 대신 무슨 일이 일어나는지 그대로 쓴다.

**과장된 소제목을 쓰지 않는다.** "가장 중요한 표", "그림이 말하지 않는 것" 대신 "ablation", "주의할 점" 처럼 내용을 가리키는 이름을 쓴다.

**em dash 를 쓰지 않는다.** 쉼표나 문장 분리로 처리한다.

**문서를 어떻게 만들었는지 문서 안에 쓰지 않는다.** "이 자료는 소개글이 아니라 원문을 직접 읽고 정리했다", "모든 수치는 표에서 직접 가져왔다" 같은 문장은 독자에게 아무 정보도 주지 않는다. 어디서 왔는지는 출처 장에 사실만 적는다. 원문에 없는 계산을 했을 때 캡션에 표시하는 것은 예외다. 그건 제작 과정 서술이 아니라 독자가 원문과 대조할 때 필요한 정보다.

## 검증

커밋 전에 두 스크립트를 돌린다.

```bash
node scripts/check-doc.mjs research/YYYY-MM-DD-slug
node scripts/build-index.mjs
```

`check-doc.mjs` 가 확인하는 것: doctype, charset, lang, viewport, 태그 균형, 외부 리소스 로드, 다크 테마 대응, img alt, meta.json 필수 필드, 파일 크기.

렌더링 화면을 눈으로 확인했는지 여부를 사용자에게 정확히 알린다. 헤드리스 브라우저를 못 써서 확인하지 못했다면 그렇게 말한다. 정적 검사를 통과한 것과 화면이 제대로 나온 것은 다르다.

## 하지 말 것

- **`index.html` 을 직접 고치지 않는다.** `scripts/build-index.mjs` 가 생성한다. 수동 편집은 다음 빌드에 사라진다.
- **`README.md` 의 문서 목록 구간을 손으로 고치지 않는다.** `<!-- docs:start -->` 와 `<!-- docs:end -->` 사이는 스크립트가 채운다.
- **문서마다 공통 템플릿을 강요하지 않는다.** 표, 차트, 다이어그램 형태가 문서마다 달라서 공통 CSS 로 묶으면 오히려 제약이 된다. 각 문서가 자기 스타일을 들고 있는 편이 낫다.
- **확인하지 않은 수치를 싣지 않는다.** 기억이나 추정으로 채우지 않는다.

## 커밋

제목은 무엇을 추가했는지 한 줄로 쓰고, 본문에 문서의 주제와 근거 출처를 남긴다. 문서 하나 추가는 커밋 하나로 묶는다.
